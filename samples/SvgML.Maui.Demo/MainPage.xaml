<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:svgml="clr-namespace:SvgML;assembly=SvgML.Maui"
             x:Class="SvgML.Maui.Demo.MainPage">

  <VerticalStackLayout Padding="30,0" Spacing="25">

    <svgml:svg viewBox="0 0 200 100">
      <svgml:defs>
        <svgml:linearGradient id="gradient" x1="0%" y1="0%" x2="0" y2="100%">
          <svgml:stop offset="0%" style="stop-color:skyblue;" />
          <svgml:stop offset="100%" style="stop-color:seagreen;" />
        </svgml:linearGradient>
      </svgml:defs>
      <svgml:rect x="0" y="0" width="100%" height="100%" fill="url(#gradient)" />
      <svgml:circle cx="50" cy="50" r="40" fill="{Binding Source={x:Reference CircleFill}, Path=Text, Mode=TwoWay}" />
      <svgml:circle cx="150" cy="50" r="40" fill="black" opacity="0.3" />
    </svgml:svg>

    <Entry x:Name="CircleFill" Text="red" />

  </VerticalStackLayout>

  
  
  
  
  <!--
  <Canvas WidthRequest="400" 
          HeightRequest="400" 
          Background="LightGray">
    <Label Text="Name" 
           WidthRequest="100" HeightRequest="32"
           Canvas.Left="50"
           Canvas.Top="50" />
    <Entry Text="Wiesław" 
           WidthRequest="100" HeightRequest="32"
           Canvas.Left="50"
           Canvas.Top="82" />
  </Canvas>
  -->

  <!--
  <VerticalStackLayout Padding="30,0" Spacing="25">

    <svg viewBox="0 0 200 100">
      <defs>
        <linearGradient id="gradient" x1="0%" y1="0%" x2="0" y2="100%">
          <stop offset="0%" style="stop-color:skyblue;" />
          <stop offset="100%" style="stop-color:seagreen;" />
        </linearGradient>
      </defs>
      <rect x="0" y="0" width="100%" height="100%" fill="url(#gradient)" />
      <circle cx="50" cy="50" r="40" fill="{Binding Source={x:Reference CircleFill}, Path=Text, Mode=TwoWay}" />
      <circle cx="150" cy="50" r="40" fill="black" opacity="0.3" />
    </svg>

    <Entry x:Name="CircleFill" Text="red" />

  </VerticalStackLayout>
  -->

  <!--
  <svg viewBox="0 0 200 100">
    <defs>
      <linearGradient id="gradient" x1="0%" y1="0%" x2="0" y2="100%">
        <stop offset="0%" style="stop-color:skyblue;" />
        <stop offset="100%" style="stop-color:seagreen;" />
      </linearGradient>
    </defs>
    <rect x="0" y="0" width="100%" height="100%" fill="url(#gradient)" />
    <circle cx="50" cy="50" r="40" fill="black" />
    <circle cx="150" cy="50" r="40" fill="black" opacity="0.3" />
  </svg>
  -->
  
  <!--
  <VerticalStackLayout Padding="30,0" Spacing="25">

    <svg viewBox="0 0 200 100">
      <defs>
        <linearGradient id="gradient" x1="0%" y1="0%" x2="0" y2="100%">
          <stop offset="0%" style="stop-color:skyblue;" />
          <stop offset="100%" style="stop-color:seagreen;" />
        </linearGradient>
      </defs>
      <rect x="0" y="0" width="100%" height="100%" fill="url(#gradient)" />
      <circle cx="50" cy="50" r="40" fill="{Binding Source={x:Reference CircleFill}, Path=Text, Mode=TwoWay}" />
      <circle cx="150" cy="50" r="40" fill="black" opacity="0.3" />
    </svg>

    <Entry x:Name="CircleFill" Text="red" />

  </VerticalStackLayout>
  -->

    <!-- xmlns:skia="clr-namespace:SkiaSharp.Views.Maui.Controls;assembly=SkiaSharp.Views.Maui.Controls" -->
    
    <!--
    <skia:SKCanvasView x:Name="skiaView"
                       PaintSurface="OnPaintSurface"
                       EnableTouchEvents="True"
                       Touch="OnTouch"
                       IgnorePixelScaling="True" />
    -->
    
    <!--
    <ScrollView>
        <VerticalStackLayout
            Padding="30,0"
            Spacing="25">
            <Image
                Source="dotnet_bot.png"
                HeightRequest="185"
                Aspect="AspectFit"
                SemanticProperties.Description="dot net bot in a race car number eight" />

            <Label
                Text="Hello, World!"
                Style="{StaticResource Headline}"
                SemanticProperties.HeadingLevel="Level1" />

            <Label
                Text="Welcome to &#10;.NET Multi-platform App UI"
                Style="{StaticResource SubHeadline}"
                SemanticProperties.HeadingLevel="Level2"
                SemanticProperties.Description="Welcome to dot net Multi platform App U I" />

            <Button
                x:Name="CounterBtn"
                Text="Click me" 
                SemanticProperties.Hint="Counts the number of times you click"
                Clicked="OnCounterClicked"
                HorizontalOptions="Fill" />
        </VerticalStackLayout>
    </ScrollView>
-->

</ContentPage>
