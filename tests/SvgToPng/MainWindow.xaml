<Window x:Class="SvgToPng.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        WindowStartupLocation="CenterScreen"
        UseLayoutRounding="True" SnapsToDevicePixels="True"
        TextOptions.TextFormattingMode="Display" TextOptions.TextRenderingMode="ClearType"
        RenderOptions.BitmapScalingMode="HighQuality"
        AllowDrop="True" DragDrop.Drop="Window_Drop"
        Title="SvgToPng" Height="650" Width="1000">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="250"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <ListBox x:Name="ListFiles" ItemsSource="{Binding Items}" Margin="4" Grid.Column="0" Grid.Row="0">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding Name}"/>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
        <Viewbox Stretch="Uniform" StretchDirection="DownOnly" Margin="4" Grid.Column="1" Grid.Row="0">
            <Image x:Name="ImagePreview" Source="{Binding ElementName=ListFiles, Path=SelectedItem.Image}"/>
        </Viewbox>
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Stretch" Margin="4" Grid.Column="0" Grid.ColumnSpan="2" Grid.Row="1">
            <TextBlock Text="Output path" Padding="0" Margin="2"/>
            <TextBox x:Name="TextOutputPath" Text="c:\DOWNLOADS\GitHub\Svg.Skia\src\Svg.Skia.Converter\bin\Debug\netcoreapp3.0\png\" Padding="0" Margin="2"/>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button x:Name="ButtonClear" Content="Clear" Padding="4" Margin="2" Width="100" Click="ButtonClear_Click"/>
                <Button x:Name="ButtonSavePng" Content="Save as Png" Padding="4" Margin="2" Width="100" Click="ButtonSavePng_Click"/>
            </StackPanel>
            <TextBlock x:Name="TextProgress" Text="" Padding="0" Margin="2"/>
            <TextBlock x:Name="TextInputFile" Text="" Padding="0" Margin="2" TextTrimming="WordEllipsis" TextWrapping="NoWrap"/>
            <TextBlock x:Name="TextOutputFile" Text="" Padding="0" Margin="2" TextTrimming="WordEllipsis" TextWrapping="NoWrap"/>
        </StackPanel>
    </Grid>
</Window>
