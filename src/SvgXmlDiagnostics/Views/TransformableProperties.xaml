<UserControl x:Class="SvgXmlDiagnostics.Views.TransformableProperties"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SvgXmlDiagnostics.Views"
             xmlns:Svg="clr-namespace:Svg;assembly=SvgXml"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto">
        <Grid Margin="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <!-- Transform -->
            <TextBlock Text="Transform" Grid.Row="0" Grid.Column="0" Margin="2" />
            <TextBox Text="{Binding (Svg:ISvgTransformableAttributes.Transform)}" Grid.Row="0" Grid.Column="1" Margin="2" />
        </Grid>
    </ScrollViewer>
</UserControl>
